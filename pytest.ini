[pytest]
addopts = --ignore=consent_api/tests/test_performance.py
markers =
    end_to_end: mark a test as an end-to-end test (uses selenium)
    integration: mark a test as an integration test
    unit: mark a test as a unit test
